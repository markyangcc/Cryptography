#include <array>
#include <iostream>
#include <string>

using namespace std;

string encryprogress(string s, array<array<char, 5>, 5> table) {
  char ch1;
  char ch2;
  for (int i = 0; i < s.length(); i++) {
    ch1 = s[i];
    ch2 = s[i + 1];
  }
}
